import React from 'react';

const TechStack: React.FC = () => (
  <div>
    <h2>Tech Stack</h2>
    <p>Java, Spring Boot, AWS Lambda, API Gateway, DynamoDB, SNS, SQS, Docker</p>
  </div>
);

export default TechStack;